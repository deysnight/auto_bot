export enum eSignal {
  GLOBALSAVEFILE = 'GlobalSaveFile',
  TASKSSAVEFILE = 'TasksSaveFile',
  PIDFILE = 'PidFile',
}

export enum ePriority {
  NONE = 0,
  LOWEST,
  LOW,
  NORMAL,
  HIGH,
  HIGHEST,
}
